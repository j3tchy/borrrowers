<table class="primary">
	<thead>
		<tr>
			<th>Name</th>
			<th>Articles</th>
			<th></th>
		</tr>
	</thead>
	<tbody>
		{{#each model as |friend|}}
		<tr>
			<td>{{link-to friend.full_name 'articles' friend}}</td>
			<td>{{friend.total_articles}}</td>
			<td><a href="#" {{action 'delete' friend}}>Delete</a></td>
		</tr>
		{{/each}}
	</tbody>
</table>
